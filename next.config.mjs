/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath:"/acesggscc.github.io",
    distDir:"./dist",
    output:"export",
    images:{
        unoptimized:true,
    }
};

export default nextConfig;
